!include opensees.yaml
!include rcic-module.yaml
---
- package: opensees module
  category: PHYSICS
  release: 2
  module:
    prepend_path:
      - PATH {{root}}/bin
      - LD_LIBRARY_PATH {{root}}/lib
    setenv:
      -  OPENSEES_DIR {{root}}
